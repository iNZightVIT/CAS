ui =
    fluidPage(
        titlePanel("Census Viewer"),
        theme = "cosmo.css",
        responsive = FALSE,
        column(width = 2,
               br(),
               wellPanel(
                   h5(helpText("Data Set ..." ... [TRUNCATED] 
ui =
    fluidPage(
        titlePanel("Census Viewer"),
        theme = "cosmo.css",
        responsive = FALSE,
        column(width = 2,
               br(),
               wellPanel(
                   h5(helpText("Data Set ..." ... [TRUNCATED] 
e
